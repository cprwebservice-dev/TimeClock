Time-Clock Enterprise V6.11.19

Frontend update only. No SQL required.

Changes:
1) Team Daily Detail > จัดกะ: assignment modal now stacks above the Team Daily drawer correctly.
2) Modal/loading/toast z-index layers normalized.
3) Dark mode synchronized across legacy and enterprise theme classes.
4) Dark UI palette improved for panels, forms, tables, modals, cards, sidebar, status chips, loading and scrollbars across all pages.
5) Theme selection in top bar and Settings now stays synchronized.
