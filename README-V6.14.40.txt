TimeAttendance V6.14.40

Person Schedule OFF Icon
- Person schedule 15-day and full-month OFF/day-off labels use a Calendar-X icon matching the supplied reference.
- OFF icon has no tile background or border in its normal state.
- Icon stroke uses currentColor and therefore follows System Settings --shift-off-text.
- Existing hover/selected/review/confirmed interaction indicators are retained.
- Other shift icons and scheduling/business rules are unchanged.
- Frontend-only; no SQL migration required.
