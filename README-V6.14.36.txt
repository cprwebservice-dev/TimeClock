TimeAttendance V6.14.36

แก้เฉพาะเงื่อนไขลำดับกะดึกตามคำยืนยันล่าสุด:
- กะดึก -> ลา LV = อนุญาต
- LV ยังคงเป็นลา ไม่ใช่วันหยุด และไม่กระทบโควต้าวันหยุด
- กฎเดิมอื่นคงเดิม: วันก่อนกะดึกห้ามเป็นกะเช้า/กลางวัน, กะดึก->กะเช้า block, พักขั้นต่ำ 6 ชม., >48 ชม. warning

ติดตั้ง:
1) รัน SQL_ที่ต้องรัน_V6.14.36_NIGHT_SEQUENCE_LV_ALLOW.sql
2) Deploy ZIP V6.14.36
3) Ctrl+F5
4) รัน SQL_สำหรับตรวจสอบ_V6.14.36_NIGHT_SEQUENCE_LV_ALLOW.sql
